% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rates_cohort.R
\name{compute_rates_cohort}
\alias{compute_rates_cohort}
\title{Wrapper function to compute statistics relating to incidence, prevalences and risks (ratios and differences)
per group and between groups}
\usage{
compute_rates_cohort(
  aesifup_input,
  fupCol = "fup",
  pyrCol = "pyr",
  pyr_offset = "pyr_offset",
  eventCol = "eventCount",
  iptw = "ip_weight",
  target_aesi,
  type = "incidence",
  CImethod = "wilson",
  risk_type = "survival",
  CumulativeInc = TRUE,
  comparison_measures = TRUE,
  scale_IR = 10000,
  model_based_control = FALSE,
  weighted_IR = FALSE,
  end_risk,
  output_format = "data.table",
  minimum_count_for_comparative = 3,
  unique_person_cols = NULL
)
}
\arguments{
\item{aesifup_input}{data.table, typically representing two groups (exposed and control), with columns specifying person-time, occurence of events, and group membership. Must contain a column \code{group} with values EXPOSED and CONTROL. May be outputted by \code{process_cohort_data}}

\item{fupCol}{name of column representing days of follow-up time. defaults to "fup"}

\item{pyrCol}{name of column representing person-years of follow-up time. defaults to "pyr"}

\item{pyr_offset}{name of column which is used as input to poisson (GEE) models if necessary, typically person-time. "pyr_offset"}

\item{eventCol}{name column identifying binary event count for this unit, "eventCount"}

\item{iptw}{name column containing (ipt or other) weights to be used in the analysis}

\item{target_aesi}{character string giving name of the outcome variable, for informative logging}

\item{type}{"incidence" or "prevalence", defining if incidence rates or prevalenes are to be computed}

\item{CImethod}{defaults to "wilson"; option passed to \code{est_inc_prev} defining formula for analytic CI computation}

\item{risk_type}{defaults "survival". if set to survival, function outputs 1-KM risk differences and ratios. Otherwise prevalence/incidence ratios and differences, as specified by \code{type}}

\item{CumulativeInc}{TRUE/FALSE, compute cumulative incidences using 1-KM. If not, skip and return -99 flag}

\item{comparison_measures}{controls whether 1-KM is based on the latest available follow-up in both exposed and control (TRUE) or just the maximum follow-up in the dataset as a whole (FALSE)}

\item{scale_IR}{what scaling to perform for incidence/prevalence, i.e., events per X. Defaults to 10000}

\item{model_based_control}{TRUE/FALSE. Use GEE to estimate incidence/prevalence in the control group (TRUE) or an analytic formula that assumes independent observations (FALSE)}

\item{weighted_IR}{use iptw column to weight the incidence/prevalence estimates per group (TRUE) or output unweighted incidence/prevalence in the exposed and control (FALSE)}

\item{end_risk}{end of risk period for computing cumulative incidence}

\item{output_format}{defaults to \code{data.table}, otherwise returns data.frame.}

\item{minimum_count_for_comparative}{defaults to 3; should a minimum event count be applied in order to display results, all estimates relating to event counts less than this will be suppressed}

\item{unique_person_cols}{optional character vector of column names to be used to identify unique patients in the dataset. If not specified, counts of patients will be based on number of rows in the dataset. If specified, counts of patients will be based on number of unique combinations of values in these columns.}
}
\description{
Wrapper function to compute statistics relating to incidence, prevalences and risks (ratios and differences)
per group and between groups
}
