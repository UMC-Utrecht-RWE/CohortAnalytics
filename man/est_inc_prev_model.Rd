% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est_inc_prev.R
\name{est_inc_prev_model}
\alias{est_inc_prev_model}
\title{Estimate model-based incidence/prevalence for a specific group from a GEE Poisson model}
\usage{
est_inc_prev_model(modelobj, group, scale_IR)
}
\arguments{
\item{modelobj}{fitted model object (typically \code{geeM::geem}) or a character error flag}

\item{group}{target group label; must be \code{"CONTROL"} or \code{"EXPOSED"}}

\item{scale_IR}{multiplicative scaling factor for the output estimate and CI bounds}
}
\value{
data.frame with \code{ir_est}, \code{ir_lb}, and \code{ir_ub}
}
\description{
Extracts a model-based incidence/prevalence estimate and Wald-type 95\% CI on the
log scale, then exponentiates and scales the result.
}
\details{
Supports two model structures:
\enumerate{
\item subgroup-specific model without a group term (intercept-only for that subgroup), and
\item model with a group term, where CONTROL is based on the intercept and EXPOSED is
based on intercept + group coefficient.
}

For EXPOSED in a model with a group term, the variance of the linear predictor is
computed as var(intercept) + var(group) + 2*cov(intercept, group).
}
